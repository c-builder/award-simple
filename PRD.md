# 部门荣誉展播系统 PRD

## 1. 产品概述

### 1.1 产品定位

部门荣誉展播系统是一款面向企业内部员工的荣誉展示管理平台，用于集中展示和管理各类获奖信息（包括个人奖和团队奖），支持跨部门协作展示，最终生成统一的获奖海报。

### 1.2 核心价值

- **统一管理**：集中管理各类获奖数据，避免信息分散
- **跨部门展示**：支持跨部门团队成员在同一奖项下展示
- **灵活筛选**：按部门筛选和过滤获奖数据
- **流程化操作**：三步骤引导式操作（选择数据→制作展播→生成展报）

### 1.3 目标用户

| 用户角色 | 使用场景 |
|---------|---------|
| 普通员工 | 查看本部门及跨部门获奖信息 |
| 部门管理员 | 选择和管理本部门展播数据、添加自定义奖项 |
| HR/行政人员 | 统一管理企业级获奖数据、生成展播海报 |

---

## 2. 功能规格

### 2.1 核心功能模块

#### 2.1.1 奖项数据管理

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 默认奖项展示 | 系统预置的近期获奖数据，按推送日期排序展示 | P0 |
| 自定义奖项添加 | 通过「添加展播奖项」弹窗自定义添加奖项 | P0 |
| 奖项删除 | 仅允许删除自定义奖项，默认奖项不可删除 | P0 |
| 奖项筛选 | 按年份、奖项名称、颁发部门进行筛选 | P1 |

#### 2.1.2 个人奖项管理

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 获奖人展示 | 以卡片形式展示获奖人姓名、工号、部门 | P0 |
| 获奖数量统计 | 显示当前部门在该奖项的获奖人数（如：1人） | P0 |
| 获奖人编辑 | 添加/移除特定获奖人 | P0 |
| 跨部门添加 | 从该奖项全部获奖人池中选择添加其他部门人员 | P0 |
| 获奖人搜索 | 支持按姓名、工号搜索获奖人 | P0 |
| 跨部门标记 | 跨部门获奖人显示特殊标识「跨」 | P0 |
| 批量选择 | 支持全选/取消全选当前页获奖人 | P1 |
| 分页展示 | 每页显示10条获奖人数据，支持分页 | P0 |

#### 2.1.3 团队奖项管理

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 团队展示 | 以卡片形式展示团队名称、成员数量 | P0 |
| 获奖团队统计 | 显示当前部门在该奖项的获奖团队数（如：3个团队） | P0 |
| 团队编辑 | 添加/移除获奖团队 | P0 |
| 跨部门添加 | 从该奖项全部获奖团队池中选择添加其他部门团队 | P0 |
| 成员详情 | 查看团队成员列表 | P0 |
| 成员管理 | 添加/移除/选择团队成员 | P0 |
| 成员搜索 | 支持按工号搜索团队成员 | P0 |
| 全选/展开 | 支持批量选择和展开查看团队成员 | P1 |

#### 2.1.4 部门筛选

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| 部门选择 | 下拉选择展示部门（全部部门/具体部门） | P0 |
| 部门数据隔离 | 仅展示当前部门及跨部门关联数据 | P0 |
| 可访问部门列表 | IT平台服务部、质量与流程IT部、智能汽车解决方案部、云与计算业务部、华为公司 | P0 |

#### 2.1.5 展播生成流程

| 步骤 | 功能 | 状态 |
|------|------|------|
| 第一步：选择展播数据 | 展示和筛选获奖数据 | 已实现 |
| 第二步：制作展播 | 展播内容编辑（占位） | 待开发 |
| 第三步：生成展播 | 生成最终海报（占位） | 待开发 |

### 2.2 用户交互流程

```
用户登录 → 首页展示默认奖项 → 切换部门筛选 → 编辑获奖人/团队 → 添加自定义奖项 → 进入展播流程
```

### 2.3 数据模型

#### 奖项 (Award)

```typescript
interface Award {
  id: string;                    // 唯一标识
  title: string;                 // 奖项名称
  issuingDepartment: string;     // 颁发部门
  awardType: 'individual' | 'team'; // 奖项类型
  recipients: Recipient[];        // 当前部门获奖人列表（个人奖）
  allRecipients?: Recipient[];    // 该奖项全部获奖人池（跨部门添加用）
  teams?: Team[];                // 当前部门获奖团队列表（团队奖）
  allTeams?: Team[];             // 该奖项全部获奖团队池（跨部门添加用）
  selected?: boolean;            // 是否选中用于展播
  isDefault?: boolean;           // 是否为默认推送数据
  pushDate?: string;             // 推送日期 (YYYY-MM-DD)
  totalRecipients?: number;      // 该奖项总获奖人数（个人奖）
  totalTeams?: number;           // 该奖项总获奖团队数（团队奖）
}
```

#### 获奖人 (Recipient)

```typescript
interface Recipient {
  name: string;                  // 姓名
  employeeId: string;           // 工号
  department: string;            // 部门路径
  isSelected?: boolean;          // 是否选中
  isManuallyAdded?: boolean;     // 是否手动添加的跨部门人员
  isCrossDepartment?: boolean;   // 是否为跨部门人员（相对于当前查看部门）
}
```

#### 团队 (Team)

```typescript
interface Team {
  id: string;                    // 团队ID
  name: string;                  // 团队名称
  leaderName?: string;           // 负责人姓名
  leaderId?: string;             // 负责人工号
  memberCount: number;           // 成员数量
  members?: TeamMember[];         // 成员列表
  isSelected?: boolean;          // 是否选中
  isManuallyAdded?: boolean;     // 是否手动添加
  isCrossDepartment?: boolean;   // 是否为跨部门团队（相对于当前查看部门）
  department?: string;           // 团队所属部门
}
```

#### 团队成员 (TeamMember)

```typescript
interface TeamMember {
  name: string;                  // 姓名
  employeeId: string;           // 工号
  department: string;            // 部门路径
  role?: string;                 // 角色（组长/架构师等）
  isSelected?: boolean;          // 是否选中
}
```

---

## 3. 界面规格

### 3.1 整体布局

| 区域 | 描述 |
|------|------|
| 顶部导航栏 | 深色背景 (#1a1a2e)，显示标题「获奖海报生成系统」和当前用户信息 |
| 步骤指示器 | 三步骤流程指示（选择展播数据→制作展播→生成展播） |
| 主内容区 | 奖项卡片列表，支持筛选和操作 |
| 底部操作栏 | 固定在底部，包含上一步/下一步/生成展播按钮 |

### 3.2 组件规格

#### 奖项卡片 (AwardCard)

- 白色背景 (#fff)，圆角 (8px)，阴影 (0 1px 3px)
- 顶部：奖项名称 + 类型标签（个人奖/团队奖）
- 副标题：颁发部门、颁奖数量
- 内容区：获奖人/团队卡片列表（最多显示10个）
- 操作按钮：编辑获奖人/团队、删除（仅自定义奖项）
- 跨部门数据：显示「跨」标识

#### 获奖人卡片 (RecipientCard)

- 浅灰背景 (#f8fafc)，边框 (#e2e8f0)
- 布局：姓名 + 工号同一行，部门在下一行
- 右上角：删除按钮（悬停显示）
- 右下角：跨部门标识（蓝色「跨」标签）

#### 团队卡片 (TeamCard)

- 与获奖人卡片样式一致
- 布局：团队名称（最多2行）+ 成员数量 + 详情按钮
- 支持悬停显示删除按钮

### 3.3 弹窗规格

| 弹窗 | 用途 | 尺寸 |
|------|------|------|
| AddAwardModal | 添加展播奖项 | 900px × 85vh |
| AddRecipientModal | 编辑获奖人 | 900px × 80vh |
| TeamSearchModal | 编辑获奖团队 | 900px × 80vh |
| TeamMembersModal | 查看/编辑团队成员 | 800px × 80vh |

---

## 4. 业务规则

### 4.1 数据展示规则

| 规则 | 说明 |
|------|------|
| 默认奖项保护 | 默认奖项不允许删除，只能由系统管理员操作 |
| 部门数据隔离 | 选择特定部门后，仅展示该部门及跨部门关联数据 |
| 跨部门标记 | 获奖人/团队的一级部门 ≠ 当前选中部门时，显示「跨」标识 |
| 手动添加标记 | 通过弹窗手动添加的跨部门人员/团队，记录 isManuallyAdded 状态 |
| 默认选中状态 | 团队奖默认全部选中，个人奖按 isSelected 字段显示 |
| 默认推送规则 | 系统根据最近获奖情况，自动推送当前部门的获奖记录到首页 |

### 4.2 获奖统计规则

#### 4.2.1 个人奖统计方法
- **统计维度**：按获奖人数量计算
- **计算示例**：如"2025年优秀员工奖"，IT平台服务部有1人获得，则该部门在此奖项的获奖数量为 **1个**
- **展示内容**：显示获奖人姓名、工号、部门信息

#### 4.2.2 团队奖统计方法
- **统计维度**：按获奖团队数量计算（非团队成员人数）
- **计算示例**：如"2025年OCC委员会2025年4月激励奖项"，IT平台服务部有3个团队获得，则该部门在此奖项的获奖数量为 **3个**
- **展示内容**：显示团队名称、成员数量

### 4.3 跨部门添加规则

#### 4.3.1 个人奖跨部门添加
- **添加范围**：只能从此奖项的所有获奖人列表中选择添加
- **前提条件**：需先获取该奖项的总获奖人数（全部部门的获奖人）
- **可选人员**：仅限该奖项已定义的获奖人池中的其他部门人员
- **示例**："2025年优秀员工奖"共有N人获奖，当前部门显示1人，可从剩余N-1人中添加其他部门获奖人

#### 4.3.2 团队奖跨部门添加
- **添加范围**：只能从此奖项的所有获奖团队列表中选择添加
- **前提条件**：需先获取该奖项的总获奖团队数（全部部门的获奖团队）
- **可选团队**：仅限该奖项已定义的获奖团队池中的其他部门团队
- **示例**："2025年OCC委员会2025年4月激励奖项"共有30个团队获奖，当前部门显示3个团队，可从剩余27个团队中添加其他部门团队

### 4.4 筛选规则

```
筛选后的奖项 = 默认奖项（按部门筛选）+ 自定义奖项（全部显示）
```

- **个人奖筛选逻辑**：获奖人部门一级路径匹配当前部门
- **团队奖筛选逻辑**：至少有一个成员的部门一级路径匹配当前部门
- **自定义奖项**：在所有部门视图中均显示

### 4.3 步骤流转规则

| 当前步骤 | 进入下一步条件 |
|----------|---------------|
| 第一步 | 至少存在一个有获奖人/团队的奖项 |
| 第二步 | 第一步已完成（占位） |
| 第三步 | 第二步已完成（占位） |

---

## 5. 技术规格

### 5.1 技术栈

| 类别 | 技术选型 |
|------|---------|
| 前端框架 | React 18.2.0 |
| 开发语言 | TypeScript 5.2.2 |
| 构建工具 | Vite 5.0.8 |
| 样式方案 | 内联样式 (CSS-in-JS) |
| 部署平台 | GitHub Pages |

### 5.2 项目结构

```
src/
├── components/           # React 组件
│   ├── AddAwardModal.tsx       # 添加奖项弹窗
│   ├── AddRecipientModal.tsx   # 编辑获奖人弹窗
│   ├── AwardCard.tsx           # 奖项卡片
│   ├── DataRangeFilter.tsx     # 部门筛选组件
│   ├── DeptCascader.tsx        # 部门级联选择器
│   ├── Pagination.tsx          # 分页组件
│   ├── RecipientCard.tsx       # 获奖人卡片
│   ├── TeamMembersModal.tsx     # 团队成员弹窗
│   ├── TeamSearchModal.tsx      # 搜索团队弹窗
│   ├── index.ts               # 组件导出
│   └── types.ts               # TypeScript 类型定义
├── mock/
│   └── data/                  # 模拟数据
│       ├── addAwardItems.json  # 可添加的奖项列表
│       ├── awards.json         # 奖项数据
│       └── employees.json      # 员工数据
├── App.tsx              # 主应用组件
├── index.css            # 全局样式
└── main.tsx            # 入口文件
```

---

## 6. 待开发功能

根据代码分析，以下功能为占位状态或待实现：

| 功能 | 当前状态 | 优先级 |
|------|---------|--------|
| 展播制作 | 第二步占位页面 | P0 |
| 展播生成 | 第三步占位页面 | P0 |
| 海报导出 | 未实现 | P1 |
| 数据持久化 | 当前为前端状态，无后端存储 | P1 |
| 用户认证 | 未实现（硬编码用户信息） | P2 |

---

## 7. 验收标准

### 7.1 功能验收

- [ ] 奖项数据按部门正确筛选
- [ ] 个人奖可添加/移除获奖人
- [ ] 团队奖可添加/移除团队
- [ ] 团队成员可查看详情并添加跨部门成员
- [ ] 自定义奖项可正常添加和删除
- [ ] 默认奖项不可删除
- [ ] 跨部门数据正确显示「跨」标识
- [ ] 分页组件正常工作

### 7.2 交互验收

- [ ] 弹窗可正常打开和关闭
- [ ] 搜索功能正常响应
- [ ] 步骤流转逻辑正确
- [ ] 下一步按钮在无数据时禁用
- [ ] 悬停效果正常显示

### 7.3 兼容性验收

- [ ] 桌面端（1920×1080）显示正常
- [ ] 平板端（768px+）布局自适应
- [ ] 主流浏览器（Chrome、Firefox、Safari、Edge）兼容

---

## 8. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| 1.0 | 2025-05-15 | 初始版本，基于代码分析生成PRD |
